server:
  port: 8080
  servlet:
    context-path: /challenge-service

spring:
  cloud:
    config:
      enabled: false
      
  redis:
    host: 'challenge-eventstore_redis_1'
#    host: 'localhost'
    database: 1
# In this case, i prefer to use Lettuce because this is an natural scalable and thread safe client for redis    
    lettuce: 
    
# Here, i use a connection pool for multithreading and reusing connections, in addition to ensure thread safe (This object is thread safe)    
      pool:
        max-active: 32
        max-idle: 2
        min-idle: 1

management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: health, info
  endpoint:
    metrics:
      enabled: false
